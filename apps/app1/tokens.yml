jwt_configuration:
  lifetime_in_seconds: 3600  # Within limits
  alg: "RS256"  # Valid

refresh_token:
  rotation_type: "rotating"
  expiration_type: "expiring"
  token_lifetime: 864  # Within limits (3 days)
