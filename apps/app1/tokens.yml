jwt_configuration:
  lifetime_in_seconds: 360000  # Too high - should fail
  alg: "RS256invalid"  # Invalid algorithm - should fail

refresh_token:
  rotation_type: "rotating"
  expiration_type: "expiring"
  token_lifetime: 2592  # Too low - should fail
