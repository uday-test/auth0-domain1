passwordPolicy:
  length:
    min: 16  # This overrides hardening.yml
  history: 10

guardianMfaPolicy:
  factors:
    - name: "otp"
      enabled: true
    - name: "webauthn-roaming"
      enabled: true
    - name: "sms"
      enabled: false